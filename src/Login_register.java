/*
 *  
 */

/**
 *
 * @author James
 */

import java.util.*;

public class Login_register {

    // This method will load all the users from a file and put them in to a list.
    public void loadAllUsers(){
        
    }
    
    //right before the program ends it will exicute this method that sends all
    // all the user information to a file for later use.
    public void userToFile(){
        
    }
    
    //I am not sure if I can get input from here or not. But I will set it us this
    // way for now if it needs to be in the main then we can  change things.
    public void NewUser(ArrayList Users){
        // ask the user for information 
        // Name, username, password, and email
        
        // then make sure that they are not in the list
        
        // if not then then create a new userID object
        
        
    }
    
    // this will get users login information and check to make sure they are in the list
    public boolean isRegisteredUser(ArrayList users){
        return true;
    }
    
    public ArrayList Users = new ArrayList();
}
